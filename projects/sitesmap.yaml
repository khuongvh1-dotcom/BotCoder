project:
  id: sitesmap
  name: SitesMap
  type: python

repo:
  owner: khuongvh1-dotcom
  name: SitesMap
  url: https://github.com/khuongvh1-dotcom/SitesMap.git
  base_branch: main

branch_prefix: "ai-task/"

labels:
  task: ai-task
  pending: pending

commands:
  install: "pip install -e ."
  test: "pytest"

paths:
  danger:
    - ".github/**"
    - "pyproject.toml"
    - "setup.py"
    - "setup.cfg"
  readonly: []

context_file: AI_PROJECT_CONTEXT.md
workspace_dir: workspace
